#ifndef ${UPPER_PROJECT_NAME}_VERSION_HPP
#define ${UPPER_PROJECT_NAME}_VERSION_HPP

#define ${UPPER_PROJECT_NAME}_VERSION "${PROJECT_VERSION}"
#define ${UPPER_PROJECT_NAME}_VERSION_MAJOR (${PROJECT_VERSION_MAJOR}+0)
#define ${UPPER_PROJECT_NAME}_VERSION_MINOR (${PROJECT_VERSION_MINOR}+0)
#define ${UPPER_PROJECT_NAME}_VERSION_PATCH (${PROJECT_VERSION_PATCH}+0)

#endif
